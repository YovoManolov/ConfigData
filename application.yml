my: 
  greeting: Hello World from config server
  list: 
    values: One, Two, Three, Four

app: 
  name: My app
  description: Wellcome to ${app.name}

managment.endpoints.web.exposure.include: "*"

spring.cloud.config.uri: http://localhost:8888